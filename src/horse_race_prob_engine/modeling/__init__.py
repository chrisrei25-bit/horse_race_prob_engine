"""modeling — Model training, calibration, and prediction.

Walk-forward validation only — no random temporal splits.
All output probabilities must be normalized at the race level.
Calibration is a first-class requirement, not optional.
"""
